% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{pl_get_logs}
\alias{pl_get_logs}
\title{Query log entries}
\usage{
pl_get_logs(
  conn,
  flow = NULL,
  status = NULL,
  from = NULL,
  to = NULL,
  limit = 50
)
}
\arguments{
\item{conn}{A connection object from \code{\link[=pl_connect]{pl_connect()}}.}

\item{flow}{Optional flow name to filter by.}

\item{status}{Optional status to filter by (\code{"SUCCESS"}, \code{"ERROR"}, or \code{"FATAL"}).}

\item{from}{Optional start timestamp (\code{POSIXct} or ISO 8601 string).}

\item{to}{Optional end timestamp (\code{POSIXct} or ISO 8601 string).}

\item{limit}{Maximum number of records to return. Default 50.}
}
\value{
A data.frame with columns: \code{id}, \code{flow} (flow name), \code{log_type},
\code{status}, \code{message}, \code{metadata}, \code{created}.
}
\description{
Returns a data.frame of log entries, optionally filtered. All filter
arguments are optional and combined with AND logic.
}
\examples{
\dontrun{
conn <- pl_connect()
pl_get_logs(conn)
pl_get_logs(conn, flow = "ectrl_data_load", status = "ERROR")
pl_get_logs(conn, from = Sys.time() - 86400, limit = 100)
}
}
