% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{pl_connect}
\alias{pl_connect}
\title{Connect to PocketBase as a regular user}
\usage{
pl_connect(url = NULL, email = NULL, password = NULL)
}
\arguments{
\item{url}{PocketBase instance URL. Defaults to \code{POCKETLOG_URL} env var.}

\item{email}{Service account email. Defaults to \code{POCKETLOG_EMAIL} env var.}

\item{password}{Service account password. Defaults to \code{POCKETLOG_PASSWORD} env var.}
}
\value{
A named list with elements \code{url} (character) and \code{token} (character JWT).
}
\description{
Authenticates against a PocketBase \code{users} auth collection and returns a
connection object for use with all daily pocketlogR functions.
}
\examples{
\dontrun{
conn <- pl_connect()
conn <- pl_connect(
  url      = "https://myapp.pockethost.io",
  email    = "service@example.com",
  password = "secret"
)
}
}
