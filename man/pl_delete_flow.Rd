% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin.R
\name{pl_delete_flow}
\alias{pl_delete_flow}
\title{Delete a flow (admin only)}
\usage{
pl_delete_flow(conn, flow, force = FALSE)
}
\arguments{
\item{conn}{A superuser connection object from \code{\link[=pl_connect_admin]{pl_connect_admin()}}.}

\item{flow}{Flow name (character).}

\item{force}{If \code{TRUE}, deletes all log entries for this flow before deleting
the flow itself. If \code{FALSE} (default), errors if log entries exist.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Deletes a flow by name. Requires a superuser connection. Because PocketBase
enforces referential integrity, any log entries referencing the flow must be
deleted first. Use \code{force = TRUE} to do this automatically.
}
\examples{
\dontrun{
conn_admin <- pl_connect_admin()
pl_delete_flow(conn_admin, "old_flow", force = TRUE)
}
}
