% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flows.R
\name{pl_create_flow}
\alias{pl_create_flow}
\title{Create a new flow}
\usage{
pl_create_flow(
  conn,
  name,
  type,
  description = NULL,
  schedule = NULL,
  depends_on = NULL
)
}
\arguments{
\item{conn}{A connection object from \code{\link[=pl_connect]{pl_connect()}}.}

\item{name}{Unique flow identifier (e.g. \code{"ectrl_data_load"}).}

\item{type}{Flow type string. See \link{pl_flow_types} for defaults, but any string is accepted.}

\item{description}{Optional human-readable description.}

\item{schedule}{Optional cron expression or human-readable schedule string.}

\item{depends_on}{Optional character vector of upstream flow names.}
}
\value{
Invisibly returns the created flow record as a list.
}
\description{
Registers a new flow in PocketBase. Errors if a flow with the same name
already exists. Validates that \code{depends_on} flows exist and that adding
them would not create a cycle in the DAG.
}
\examples{
\dontrun{
conn <- pl_connect()
pl_create_flow(conn, "ectrl_data_load", type = "data_job",
               description = "Daily EUROCONTROL data import",
               schedule = "0 6 * * *")
}
}
