% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deps.R
\name{pl_get_dag}
\alias{pl_get_dag}
\title{Get a full DAG overview of all flows and their health}
\usage{
pl_get_dag(conn, since = NULL)
}
\arguments{
\item{conn}{A connection object from \code{\link[=pl_connect]{pl_connect()}}.}

\item{since}{Optional \code{POSIXct} or ISO 8601 string. If provided, only
considers log entries created after this timestamp.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{flow} (character): flow name
\item \code{type} (character): flow type
\item \code{schedule} (character): schedule string
\item \code{raw_status} (character): most recent log status (\code{NA} if never logged)
\item \code{raw_status_time} (character): timestamp of most recent log
\item \code{effective_status} (character): \code{"SUCCESS"}, \code{"ERROR"}, \code{"FATAL"},
\code{"POISONED"}, or \code{NA} if never logged
\item \code{poisoned_by} (list-column): character vector of upstream flow names
that caused poisoning
\item \code{depends_on} (list-column): character vector of direct upstream flow names
\item \code{is_root} (logical): \code{TRUE} if the flow has no upstream dependencies
}
}
\description{
Returns a tibble (data.frame) of ALL flows with both their raw (most recent
log) status and effective (cascade-aware) status. A flow is considered
"poisoned" if any upstream dependency has a more recent ERROR/FATAL log
than the flow's own last SUCCESS log — meaning the flow ran successfully
before the upstream broke.
}
\details{
\strong{Poisoning rule:} A downstream flow is poisoned if:
\enumerate{
\item It has at least one upstream flow with an ERROR or FATAL log.
\item The downstream's last SUCCESS log is MORE RECENT than that upstream failure.
(i.e., the downstream ran successfully after the upstream had already broken.)
\item The downstream itself is not already ERROR or FATAL.
}

If the downstream has never logged, \code{effective_status} is \code{NA} (unknown),
regardless of upstream status.
}
\examples{
\dontrun{
conn <- pl_connect()
dag <- pl_get_dag(conn)
dag
}
}
