# Default flow types

A character vector of the default flow type identifiers used by
pocketlogR. These are not enforced — any string is accepted as a type.
They exist for documentation, consistency, and convenience.

## Usage

``` r
pl_flow_types
```

## Format

An object of class `character` of length 5.
